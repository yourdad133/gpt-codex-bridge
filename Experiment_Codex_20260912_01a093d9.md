# 实验项目 Codex 回复

- 日期：2026-09-12
- 项目：实验
- 归档规则：每次最终回复新建一个 Markdown 文件，不覆盖既有归档

## 本次请求

将 X 盘作为服务器文件编辑入口，将 SSH 作为服务器 GPU 运行入口；所有代码使用远程 GPU，运行环境使用服务器中的 `tslib`，并让“实验”项目中的工作遵循这一约定。

## 已完成

- 在项目根目录加入 `AGENTS.md`，约定 X 盘负责文件修改、SSH 负责运行代码，禁止回退到本机 GPU/ Python。
- 加入 `.codex/config.toml`，记录远程运行模式、`tslib` 环境和回复归档仓库。
- 两个规则文件已提交到 `yourdad133/Time-Series-Library`，并同步到服务器项目目录；同步过程保留了原有未提交文件。
- 本次回复按要求新建此归档文件。

## 运行验证

- 远程主机可见两张 NVIDIA GeForce RTX 4090 D。
- 驱动版本：570.211.01。
- `tslib` 使用 Python 3 环境中的 PyTorch 2.5.1+cu121。
- `torch.cuda.is_available()` 为 True，设备数为 2，并已完成 CUDA 矩阵计算测试。

## 应用层说明

仓库级规则已经落地；Codex 桌面端“实验”项目本身的运行主机绑定仍需在设置中保存为 SSH 远程项目。当前接口不能直接改写该项目绑定，也不能替代远端 Codex 安装/认证。完成该一次性设置后，新建或继续的任务即可由远程项目执行；在此之前，规则要求不在本机运行计算代码。

## 安全处理

此公共归档不包含密码、令牌、私钥或内部连接细节。
